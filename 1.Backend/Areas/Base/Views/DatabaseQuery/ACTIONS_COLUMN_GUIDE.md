# ?? Database Query Tool - H??ng D?n S? D?ng

## ? ?ã S?a & Hoàn Thành

### 1. **L?i Font Ti?ng Vi?t** ?
- T?t c? text hi?n th? ?úng ti?ng Vi?t
- Files ?ã ???c save v?i UTF-8 encoding

### 2. **Hàm `showTableInfo`** ?  
- ?ã thêm vào `Index.cshtml`
- Click "INFO" bên c?nh table name ?? xem chi ti?t

### 3. **C?t Actions trong B?ng K?t Qu?** ?
- **M?I ROW** s? có 3 nút: [INSERT] [EDIT] [DELETE]
- C?t ???c thêm t? ??ng khi ch?y SELECT query

---

## ?? Cách S? D?ng C?t Actions

### Khi Ch?y Query SELECT:

```sql
SELECT TOP 100 * FROM [adm].[User]
```

**K?t qu? s? hi?n th?:**

| Id | Name | Email | **Actions** |
|----|------|-------|-------------|
| 1 | John | john@... | [INSERT] [EDIT] [DELETE] |
| 2 | Jane | jane@... | [INSERT] [EDIT] [DELETE] |

---

## ?? [INSERT] Button

### Tính n?ng:
- Copy d? li?u c?a row hi?n t?i thành câu INSERT m?i
- Gi? nguyên t?t c? values

### Cách dùng:
1. Click **[INSERT]** ? row mu?n copy
2. Popup hi?n th?:
   ```sql
   INSERT INTO [adm].[User] (
       [Id],
       [Name],
       [Email]
   ) VALUES (
       1, -- Id
       N'John', -- Name  
       N'john@test.com' -- Email
   );
   ```
3. Ch?n action:
   - **?? Copy to Clipboard**: Copy ?? paste vào SSMS
   - **?? Copy to Editor**: Copy vào editor, s?a r?i ch?y

### Use Cases:
- ? Duplicate m?t user v?i data t??ng t?
- ? T?o test data t? data th?t
- ? Migrate data gi?a các tables

---

## ?? [EDIT] Button

### Tính n?ng:
- **Option 1**: Edit tr?c ti?p trong form
- **Option 2**: Gen UPDATE query ?? review tr??c khi ch?y
- **Option 3**: Execute UPDATE ngay l?p t?c

### Cách dùng:
1. Click **[EDIT]** ? row c?n s?a
2. Popup hi?n th? UPDATE query:
   ```sql
   UPDATE [adm].[User]
   SET
       [Name] = N'John',
       [Email] = N'john@test.com'
   WHERE
       [Id] = 1;
   ```
3. Ch?n action:
   - **?? Edit Tr?c Ti?p**: M? form edit, s?a t?ng field
   - **?? Copy Query**: Copy query ?? review
   - **?? Execute Now**: Ch?y UPDATE ngay (có confirm)

### Use Cases:
- ? S?a nhanh 1 field (email, phone, status)
- ? Review UPDATE query tr??c khi ch?y
- ? Batch update nhi?u rows

---

## ?? [DELETE] Button

### Tính n?ng:
- Preview toàn b? data c?a row tr??c khi xóa
- Gen DELETE query v?i WHERE ??y ??
- Double confirmation ?? tránh xóa nh?m

### Cách dùng:
1. Click **[DELETE]** ? row c?n xóa
2. Popup hi?n th?:
   - ?? **C?nh báo màu ??**
   - **Row data preview**:
     ```
     Id: 123
     Name: Test User
     Email: test@example.com
     ```
   - **DELETE query**:
     ```sql
     DELETE FROM [adm].[User]
     WHERE
         [Id] = 123 AND
         [Name] = N'Test User' AND
         [Email] = N'test@example.com';
     ```
3. Ch?n action:
   - **?? Copy Query**: Copy ?? review tr??c
   - **??? Execute Delete**: Xóa ngay (double confirm)

### Safety Features:
- ? Hi?n th? toàn b? data tr??c khi xóa
- ? WHERE clause bao g?m T?T C? columns
- ? Double confirmation
- ? Preview query tr??c khi execute

---

## ?? Workflows Th?c T?

### Scenario 1: T?o User M?i T? User C?
```
1. SELECT * FROM [adm].[User] WHERE Id = 1
2. Click [INSERT] ? row c?a user m?u
3. Copy to Editor
4. S?a Id = 999, Name = 'New User', Email = 'new@test.com'
5. F5 th?c thi ? User m?i ???c t?o!
```

### Scenario 2: S?a Email Nhanh
```
1. SELECT * FROM [adm].[User] WHERE Name = 'John'
2. Click [EDIT]
3. Click "Edit Tr?c Ti?p"
4. S?a Email field
5. Click L?u ? Done!
```

### Scenario 3: Xóa Test Data An Toàn
```
1. SELECT * FROM [adm].[User] WHERE Email LIKE '%test%'
2. Review danh sách test users
3. Click [DELETE] ? t?ng test user
4. Xem preview data
5. Confirm 2 l?n ? Test data ???c xóa
```

### Scenario 4: Review UPDATE Tr??c Khi Ch?y
```
1. SELECT * FROM [adm].[Settings]
2. Click [EDIT] ? setting c?n ??i
3. Click "Copy Query"
4. Review query trong editor
5. S?a SET values n?u c?n
6. F5 th?c thi
```

---

## ?? Visual Guide

### C?t Actions Trong B?ng:
```
??????????????????????????????????????????????????????????
? Id ? Name  ? Email       ? Actions                    ?
?????????????????????????????????????????????????????????
? 1  ? John  ? john@... ? [INSERT] [EDIT] [DELETE]   ?
? 2  ? Jane  ? jane@... ? [INSERT] [EDIT] [DELETE]   ?
? 3  ? Bob   ? bob@...  ? [INSERT] [EDIT] [DELETE]   ?
?????????????????????????????????????????????????????????
```

### Popup INSERT:
```
???????????????????????????????????????????????
? ?? INSERT Query                             ?
???????????????????????????????????????????????
? Copy d? li?u c?a row này thành INSERT m?i   ?
?                                             ?
? [SQL Preview v?i syntax highlighting]      ?
?                                             ?
? INSERT INTO [adm].[User] (                  ?
?     [Id], [Name], [Email]                   ?
? ) VALUES (                                  ?
?     1, N'John', N'john@test.com'            ?
? );                                          ?
?                                             ?
? [?? Copy to Clipboard] [?? Copy to Editor]  ?
???????????????????????????????????????????????
```

### Popup EDIT:
```
???????????????????????????????????????????????
? ?? UPDATE Query                             ?
???????????????????????????????????????????????
? Ch?nh s?a tr?c ti?p ho?c gen UPDATE query  ?
?                                             ?
? [SQL Preview]                               ?
?                                             ?
? UPDATE [adm].[User]                         ?
? SET [Name] = N'John', [Email] = N'...'      ?
? WHERE [Id] = 1;                             ?
?                                             ?
? [?? Edit] [?? Copy] [?? Execute Now]        ?
???????????????????????????????????????????????
```

### Popup DELETE:
```
???????????????????????????????????????????????
? ?? DELETE Query                             ?
???????????????????????????????????????????????
? C?nh báo: Thao tác này s? XÓA d? li?u!     ?
?                                             ?
? ?? Row data:                                ?
? Id: 123                                     ?
? Name: John                                  ?
? Email: john@test.com                        ?
?                                             ?
? [SQL Preview]                               ?
?                                             ?
? DELETE FROM [adm].[User]                    ?
? WHERE [Id] = 123 AND ...                    ?
?                                             ?
? [?? Copy Query] [??? Execute Delete]        ?
???????????????????????????????????????????????
```

---

## ?? Cách Ki?m Tra Ch?c N?ng

### Test INSERT:
```sql
-- 1. Ch?y query
SELECT TOP 1 * FROM [adm].[User]

-- 2. Ki?m tra:
-- ? Th?y c?t "Actions" ? cu?i b?ng?
-- ? Có 3 nút [INSERT] [EDIT] [DELETE]?

-- 3. Click [INSERT]
-- ? Popup hi?n ra v?i INSERT query?
-- ? Query có ??y ?? columns và values?
-- ? Click "Copy to Editor" ? Query copy vào editor?

-- 4. S?a Id và Email, sau ?ó F5
-- ? Row m?i ???c t?o?
```

### Test EDIT:
```sql
-- 1. Ch?y query
SELECT * FROM [adm].[User] WHERE Id = 1

-- 2. Click [EDIT]
-- ? Popup hi?n UPDATE query?

-- 3. Click "Edit Tr?c Ti?p"
-- ? Form edit hi?n ra?
-- ? Có input cho t?ng column?

-- 4. S?a Name, click L?u
-- ? Data ???c update?
-- ? B?ng refresh v?i data m?i?
```

### Test DELETE:
```sql
-- 1. T?o test user tr??c
INSERT INTO [adm].[User] (Name, Email) 
VALUES (N'Test Delete', N'delete@test.com');

-- 2. SELECT l?i
SELECT * FROM [adm].[User] WHERE Email = 'delete@test.com'

-- 3. Click [DELETE]
-- ? Popup c?nh báo màu ???
-- ? Preview data ??y ???
-- ? DELETE query ?úng?

-- 4. Click "Execute Delete"
-- ? Confirm 2 l?n?
-- ? Row b? xóa?
```

---

## ?? Troubleshooting

### Không th?y c?t Actions?
**Nguyên nhân**: Query không có FROM clause
**Gi?i pháp**: 
```sql
-- ? SAI
SELECT 1 AS Test

-- ? ?ÚNG
SELECT * FROM [schema].[table]
```

### Nút không ho?t ??ng?
**Ki?m tra**:
1. F12 ? Console ? Có l?i JS không?
2. File `database-query-extended.js` ?ã ???c include?
3. Refresh page (Ctrl+F5)

### Query không detect ???c table?
**Nguyên nhân**: Format query không chu?n
**Gi?i pháp**:
```sql
-- ? SAI
SELECT * FROM User

-- ? ?ÚNG  
SELECT * FROM [adm].[User]
-- ho?c
SELECT * FROM adm.User
```

### Execute không ho?t ??ng?
**Ki?m tra**:
1. API `/Admin/Base/DatabaseQuery/UpdateRow` có ho?t ??ng?
2. Check Network tab trong F12
3. Có l?i permission không?

---

## ?? Tips & Best Practices

### ? DOs:
- ? **Test v?i SELECT tr??c**: Luôn SELECT ?? xem data tr??c khi INSERT/UPDATE/DELETE
- ? **Review query**: Click "Copy Query" ?? review tr??c khi Execute
- ? **Backup data**: Ch?y INSERT copy data quan tr?ng tr??c khi DELETE
- ? **Use WHERE c?n th?n**: ??m b?o WHERE clause chính xác

### ? DON'Ts:
- ? **DELETE không WHERE**: Tuy?t ??i không DELETE toàn b? table
- ? **Execute blind**: Không Execute mà không review query
- ? **Skip confirmation**: ??c k? confirmation message
- ? **Production data**: C?n th?n v?i production database

---

## ?? Support

N?u g?p v?n ??:
1. Check Console (F12) xem có l?i JS không
2. Check Network tab xem API có l?i không
3. Verify r?ng b?n có quy?n Super Admin
4. Clear browser cache và refresh (Ctrl+F5)

---

## ?? T?ng K?t

Database Query Tool hi?n ?ã có **??y ??** ch?c n?ng:

| Feature | Status | Description |
|---------|--------|-------------|
| C?t Actions | ? | T? ??ng thêm khi SELECT |
| INSERT Button | ? | Copy row thành INSERT m?i |
| EDIT Button | ? | Edit form ho?c gen UPDATE |
| DELETE Button | ? | Delete ho?c gen DELETE query |
| Query Preview | ? | Syntax highlighting |
| Copy Options | ? | Clipboard & Editor |
| Execute Now | ? | UPDATE/DELETE tr?c ti?p |
| Safety Features | ? | Double confirm, preview |
| showTableInfo | ? | Xem chi ti?t table |

**T?t c? ?ã ho?t ??ng!** B?n có th? b?t ??u s? d?ng ngay! ??
